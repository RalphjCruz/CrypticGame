import type { Puzzle } from "../types/puzzle";

// Legacy generated bank is intentionally not used by the game runtime.
// CrypticForge now uses `approvedManualPuzzles` from `manualPuzzles.ts`.
export const generatedPuzzles: Puzzle[] = [];
